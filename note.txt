# Life RPG - Todo

## ✅ Implémenté
- Anti-triche : quêtes < 3min non validables
- Animation célébration réussite quête
- Pénalité quêtes quotidiennes non faites (minuit)
- Système économique (pièces, boutique)
- Notifications push proactives
- Badges et trophées FOMO (durée limitée)
- Weekly summary (résumé visuel semaine)
- Daily reward / Check-in bonus (7 jours croissants)
- Streak freeze (achat en pièces, compteur de jours)
- Coffres / Loot boxes
- Widget streak (home screen)
- Sound design + haptics désactivables
- Sélecteur de skin (déblocage progressif + boutique)
- Multiplicateur de streak (momentum) : +0.1/jour, max x2.0
- Palier de streak avec récompenses fixes (7/14/30/60/90/180/365j)
- Question du soir : journal quotidien + 10 pièces (notif 21h)
- Widget Android natif (stats temps réel + ajout quête)
- Personnage pixel-art 12×16 (28 pièces, 8 catégories, rendu CustomPainter)
- Correction bug avatar tenue (clé `outfit_$ot` → `ot`)
- Sprites cheveux redessinés + ordre dessin hair avant face
- Coffres : passage en ouverture manuelle (bouton Ouvrir + animation + écran récompense)
- Historique du soir (consultation des entrées passées)
- Boutique : protection achat déjà possédé (guard + badge "Possédé ✓")
- Confirmation achat gel de série (AlertDialog)
- remettre la catégories quotidien pour les quête quotidienne et les trouver plus facillement
- corriger "cat" au début du nom des compétence 
- mettre les widget mieux
- les quêtes quotidienne doivent ce re mettre chaque jour donc le lendemin elle doivent être décocher
- mettre à quoi correspond les widget
- décochage quête : XP symétrique (avec multiplicateur) + pièces retirées + loot box progress décrémentée
- décocher n'enlève pas ce que cocher la quête a fait gagner
- Triple `_saveAllToHive()` → param `save: false` dans les sous-méthodes, 1 seul save final
- `addCoins(-n)` → `max(0, coins + amount)` pour éviter pièces négatives
- `checkDailyPenalties()` → streakFreezeDaysLeft plus perdu (variables locales, 1 seul copyWith)
- `openLootBox()` → badge plus écrasé (copyWith unique user + lootBoxProgress)
- `deleteQuest()` → `cancelReminder()` ajouté avant suppression
- weeklyXpLog → reset automatique à chaque lundi (champ `lastWeeklyLogWeekStart`)
- `canUnlockPart()` → retourne false si `cost > 0` (pièces payantes plus gratuites)
- Notification ID → `millisecondsSinceEpoch % 2^31` au lieu de `millisecond` (0-999)
- Pari auto-gagné → skip si `linkedQuestIds` vide
- Reorder quêtes → passage par IDs au lieu d'indices (plus de bug filtré/global)
- Barre progression pari → durée réelle `deadline.difference(createdAt)` au lieu de 7j
- Célébration en double → flag `_celebrationShown`
- `buyItem()` → notification "Pas assez de pièces" via NotificationService, SnackBar trompeur supprimé
- Enums en JSON → stockés par `name` au lieu d'`index` (rétrocompatible `byName`)
- Badges revérifiés au démarrage (`Future.microtask(() => checkBadges())` dans `build()`)
- Timer bets_screen → `Timer?` au lieu de `late Timer`
- `weekly_summary_screen` → `xpLog.isEmpty ? 100 : reduce(...)` évite le crash
- Visage par-dessus cheveux → ordre rendu inversé (face avant hair)
- "skills" en français → `'Compétences'`
- `settings_provider._save()` → `async` + `await` + `try-catch`
- Streak > 365j → `orElse` retourne `user.streak` au lieu du dernier milestone
- `offline_manager.saveData()` → try-catch ajouté
- 4 champs non sérialisés (guild, leaderboard) → ajoutés à toJson/fromJson
- Heure reminder → `.padLeft(2, '0')`
- `_isSameDay` dupliqué → factorisé dans `lib/core/utils.dart`
- Notification `hashCode` → `& 0x7FFFFFFF`
- Particules overlay → taille écran capturée une seule fois (`didChangeDependencies`)





##  Restant / À faire
###  Tests
- game_provider_test.dart : crash direct (pas de Hive.init)
- widget_test.dart : crash direct (pas de NotificationService.init)

### Guildes
- Revoir le système de guildes (non fonctionnel, stubs vides)

### Améliorations
- Déplacer la logique de notification dans un service dédié
